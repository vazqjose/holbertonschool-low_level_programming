#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * strtow - splits a string into words
 * @str: input string
 * Return: Null if str is null
 */

ar **strtow(char *str)
{
	int i;

	if (str == NULL || str == "")
		return (NULL);
}
