#include "holberton.h"

/**
 * is_palindrome - returns 1 if a string is palindrome and 0 if not
 * @s: string inputted
 * Return: 1 or 0
 */

int is_palindrome(char *s)
{

	if (s == '\0')
	{
		return (1);
	}

	return (0);
}
