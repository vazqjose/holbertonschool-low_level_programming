#include "holberton.h"

/**
 * _abs - computes absolute value of integer
 *
 * Return: Absolute value of integer provided
 */

int _abs(int n)
{
	int num = -1;

	if (n < 0 )
	{
		return (n*num);
	}
	else
	{
		return n;
	}
}
